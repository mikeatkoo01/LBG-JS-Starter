// function intros () {

//  1 function / const 
//  2 name the function 
//  3( set the paramiters)
//  4 => arrow marker 
//  5 values and what they need to 



// // this is to log this on the console

function sum (num, num2) {
    return num + num2;}

// thid is to return the value instead of log the value
const add = function (num, num2);
    return (num + num2);
}

function sum2 (num, num2) {
    return num / num2;

}
const sum2 = function (num, num2) {
    return (num / num2);
}
const div = (a, b) => a / b;

function sum3 (num, num2) {
    return num * num2;

}
const sum3 = function (num, num2) {
    return (num * num2);

}

const div2 = (a,b) => a * b;

const math = (a,b) => a + b;

const product = (a, b) => a * b;

function sum4 (num, num2) {
    return num - num2;



// then put in the numbers in the console

function call(num) {
    if (num % 3 === 0 && num % 5 === 0) {
        return "FizzBuzz";
    } else if (num % 3 === 0) {
        return "Fizz";
    } else if (num % 5 === 0) {
        return "Buzz";
    } else {
        return num;
    }
}



